[project]
name = "caspase"
version = "0.1.0a0"
description = "Caspase SDK — apoptosis protocol core: watcher state, symptom checks, death certificates, and control-plane client"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
dependencies = [
    "pydantic>=2.8",
    "httpx>=0.27",
    "typer>=0.12",
    "rich>=13.7",
]

[project.scripts]
caspase = "caspase.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/caspase"]
